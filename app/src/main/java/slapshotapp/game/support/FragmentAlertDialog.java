package slapshotapp.game.support;

public interface FragmentAlertDialog {
  /*
   * Callback for the Alert Dialog fragment.
   *
   * @param buttonNum The button number the callback is triggered by
   * @see MyAlertDialogFragment
   */
  void alertDialogButtonClick(int buttonNum);
}
